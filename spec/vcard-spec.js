import fs from "fs";

describe("Program testing of search functionality", function () {
  beforeAll(function () {});

  it("should create a vcard", async function () {
    const { createVcard } = await import("../src/vcard.js");

  });
});